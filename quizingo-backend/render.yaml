services:
  - type: web
    name: quizingo-backend
    runtime: python
    buildCommand: pip install fastapi uvicorn httpx websockets
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"